// 이 파일은 함수의 선언(Declaration)이 있는 곳입니다.
// C언어에서 프로젝트가 여러 .c 파일로 나뉘게 되면, 각 파일은 독립적으로 컴파일됩니다.
// 그래서 main.c를 컴파일할 때 컴파일러는 uart_puts_sync라는 함수가 실제로 어떻게 구현되었는지 전혀 모르고,
// 그저 어딘가에 존재한다고 믿고 컴파일을 진행할 뿐입니다.
// defs.h와 같은 헤더 파일은 어딘가에 구현되어 있을 함수들의 명세서 또는 목차와 같습니다.

// << printf.c >>
// UART 초기화: ns16550a를 8N1, FIFO 사용, 인터럽트 미사용(폴링) 구성으로 둡니다.
// 왜 필요한가? 부팅 직후 레지스터 상태는 구현체마다 다르고, LCR(라인 제어)/FCR(피포)/IER(인터럽트)를
// 명시적으로 설정해 두지 않으면 LSR(송신 버퍼 비었는지) 판독이 0으로 고정되어 영원히 대기할 수 있습니다.
void uart_init(void);

// 단일 문자를 동기적(폴링) 방식으로 한 바이트 출력합니다.
// 이름은 puts지만 구현은 putc에 해당합니다. 하위 호환을 위해 이름을 유지합니다.
void uart_puts_sync(unsigned char c);
