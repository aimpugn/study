# 툴 콜링은 실제로 어떻게 이뤄지는가

## 짧은 답

**모델은 도구를 실행하지 않습니다.** 약속된 형식의 문자열을 생성하고 멈춥니다. 실행은 바깥의 프로그램이 하고, 그 결과를 대화 끝에 붙여서 다시 통째로 보냅니다. 모델은 그 결과를 처음 보는 텍스트로 읽습니다.

"에이전트가 알아서 도구를 호출해준다"는 설명이 부족한 이유가 여기 있습니다. 그 문장에는 **누가 실행하는가**가 빠져 있고, 그래서 도구가 실패하면 무엇을 돌려줘야 하는지, 도구 설명문이 왜 중요한지, 왜 매 요청마다 도구 정의가 다시 실려 가는지를 설명할 수 없습니다.

## 이 문서가 답하는 것

- 도구 정의는 어떤 모습으로 모델에게 전달되는가
- 모델이 도구를 쓰겠다고 결정하면 응답이 어떤 상태로 멈추는가
- 실행 결과는 어떤 경로로 대화에 다시 들어가는가
- 도구가 실패하면 무엇을 돌려줘야 하는가
- 여러 도구를 한 번에 호출하면 결과를 어떻게 돌려줘야 하는가
- 도구 설명문이 왜 프롬프트 엔지니어링인가
- 이 모든 것이 프롬프트 캐시와 어디서 만나는가

## 목차

- [1. 모델이 실제로 받는 것은 JSON이 아니다](#1-모델이-실제로-받는-것은-json이-아니다)
- [2. 도구 정의는 시스템 메시지 안에 텍스트로 들어간다](#2-도구-정의는-시스템-메시지-안에-텍스트로-들어간다)
- [3. 왕복 한 바퀴 전체](#3-왕복-한-바퀴-전체)
- [4. 모델이 멈추는 지점](#4-모델이-멈추는-지점)
- [5. 결과가 돌아가는 길](#5-결과가-돌아가는-길)
- [6. 도구 설명문이 프롬프트인 이유](#6-도구-설명문이-프롬프트인-이유)
- [7. 도구가 실패하면](#7-도구가-실패하면)
- [8. 여러 도구를 한 번에 호출하면](#8-여러-도구를-한-번에-호출하면)
- [9. 프롬프트 캐시와 만나는 지점](#9-프롬프트-캐시와-만나는-지점)
- [10. 흔한 오해](#10-흔한-오해)
- [부록 A. 재현 방법](#부록-a-재현-방법)
- [부록 B. 확인 시점과 출처](#부록-b-확인-시점과-출처)
- [스스로 확인할 질문](#스스로-확인할-질문)

---

## 1. 모델이 실제로 받는 것은 JSON이 아니다

우리가 API로 보내는 건 이렇게 생긴 구조화된 데이터입니다.

```json
{
  "messages": [
    { "role": "user", "content": "서울 날씨 알려줘" }
  ],
  "tools": [
    {
      "type": "function",
      "function": {
        "name": "get_weather",
        "description": "특정 도시의 현재 날씨를 조회한다.",
        "parameters": {
          "type": "object",
          "properties": {
            "city": { "type": "string", "description": "도시 이름. 예: 서울" }
          },
          "required": ["city"]
        }
      }
    }
  ]
}
```

`role`이라는 필드가 있고, `tools`라는 별도 배열이 있습니다. 마치 도구가 대화와는 다른 통로로 전달되는 것처럼 보입니다.

그런데 **모델은 이 JSON을 보지 않습니다.** 모델이 받는 건 처음부터 끝까지 이어진 문자열 하나입니다. 정확히는 그 문자열을 토큰이라는 단위로 쪼갠 숫자 배열 하나이고, `role`도 `tools`도 그 안에는 없습니다.

그렇다면 JSON은 어디서 문자열이 될까요. **채팅 템플릿**이 그 일을 합니다. 열린 가중치 모델이면 이게 모델 파일 안에 같이 들어 있습니다 — `tokenizer_config.json`의 `chat_template` 필드에 Jinja2 템플릿으로 박혀 있습니다. 모델을 만든 쪽이 "우리 모델은 대화를 이런 문자열 모양으로 봐야 한다"고 정해서 가중치와 함께 배포하는 겁니다.

Qwen2.5-7B-Instruct의 템플릿에 위 JSON을 넣고 돌리면 아래 757자가 나옵니다. 이게 모델 앞에 실제로 놓이는 전부입니다.

```text
<|im_start|>system
You are Qwen, created by Alibaba Cloud. You are a helpful assistant.

# Tools

You may call one or more functions to assist with the user query.

You are provided with function signatures within <tools></tools> XML tags:
<tools>
{"type": "function", "function": {"name": "get_weather", "description": "특정 도시의 현재 날씨를 조회한다.", "parameters": {"type": "object", "properties": {"city": {"type": "string", "description": "도시 이름. 예: 서울"}}, "required": ["city"]}}}
</tools>

For each function call, return a json object with function name and arguments within <tool_call></tool_call> XML tags:
<tool_call>
{"name": <function-name>, "arguments": <args-json-object>}
</tool_call><|im_end|>
<|im_start|>user
서울 날씨 알려줘<|im_end|>
<|im_start|>assistant
```

한 번 보고 나면 되돌아가기 어렵습니다. 도구 정의가 별도 통로로 가는 게 아니라 **시스템 메시지 본문 안에 JSON이 텍스트로 그냥 박혀 있습니다.**

---

## 2. 도구 정의는 시스템 메시지 안에 텍스트로 들어간다

위 출력을 네 조각으로 나눠서 각각이 무슨 일을 하는지 보겠습니다.

**첫째, 역할 구분자.** `<|im_start|>` 와 `<|im_end|>` 는 특수 토큰입니다. 토크나이저가 이 문자열을 하나의 토큰 번호로 바꿉니다. 이 두 표시 덕분에 모델은 "여기부터 시스템, 여기부터 사용자"를 구분합니다. `role: "system"`이라는 JSON 필드가 텍스트 세계에서 어떤 모습이 되는지가 여기 있습니다 — 필드가 아니라 **구분자 토큰**입니다.

**둘째, 도구가 있다는 안내문.**

```text
# Tools

You may call one or more functions to assist with the user query.

You are provided with function signatures within <tools></tools> XML tags:
```

평범한 영어 문장입니다. 모델 제작자가 학습 때 이런 문장 뒤에 도구 목록이 온다고 가르쳐 뒀기 때문에 이 문장이 신호로 작동합니다.

**셋째, 도구 정의 그 자체.** `<tools>` 와 `</tools>` 사이에 우리가 보낸 JSON이 `tojson` 필터를 거쳐 한 줄 문자열로 직렬화돼 들어갑니다. `description` 필드에 우리가 쓴 한국어 문장 "특정 도시의 현재 날씨를 조회한다."가 그대로 보입니다. **도구 설명문은 프롬프트 본문입니다.** 6절에서 이 사실의 결과를 다룹니다.

**넷째, 출력 형식 지시.**

```text
For each function call, return a json object with function name and arguments within <tool_call></tool_call> XML tags:
<tool_call>
{"name": <function-name>, "arguments": <args-json-object>}
</tool_call>
```

여기가 결정적입니다. 이건 **명령이지 강제가 아닙니다.** 모델은 "이런 모양으로 써 달라"는 부탁을 받았을 뿐이고, 실제로 그 모양을 지킬지는 학습된 확률 분포에 달려 있습니다. 형식을 진짜로 강제하려면 샘플링 단계에서 문법에 맞지 않는 토큰을 아예 못 고르게 막아야 하는데, 그건 이 문서의 범위 밖입니다.

**마지막 줄에 주목하십시오.** 출력이 `<|im_start|>assistant\n` 로 끝납니다. 이건 모델의 응답이 아니라 **모델이 이어서 쓰도록 미리 깔아 둔 발판**입니다. 모델이 하는 일은 이 문자열 뒤에 올 다음 토큰을 하나씩 예측하는 것뿐입니다. 그러니 "도구를 호출한다"는 건 정확히 말해 **`<tool_call>` 로 시작하는 문자열을 이어 쓴다**는 뜻입니다.

---

## 3. 왕복 한 바퀴 전체

이제 전체 흐름을 봅니다. 세로 점선이 모델과 바깥 세계의 경계입니다.

```text
  내 코드 / 하네스              │ 모델
                               │
 ① 요청 JSON 조립               │
    messages + tools           │
         │                     │
 ② 채팅 템플릿으로 렌더링        │
    JSON ──> 문자열 하나        │
         │                     │
         └───── 토큰 배열 ─────>│ ③ 이 텍스트를 처음부터 읽는다
                               │        │
                               │        ▼
         <──── 생성 텍스트 ─────┤ ④ 다음 토큰을 하나씩 이어 쓴다
         │                     │    <tool_call>{"name": ...}</tool_call>
 ⑤ 파싱                        │
    문자열 ──> 구조화 블록       │
         │                     │
 ⑥ 응답 JSON을 받는다            │
    stop_reason = tool_use     │
         │                     │
 ⑦ 내가 함수를 실행한다          │   ← 모델은 여기에 관여하지 않는다
         │                     │
 ⑧ 결과를 대화 끝에 붙여서       │
    ①로 돌아간다                │
```

두 가지를 확인하십시오.

**모델이 하는 일은 ③→④뿐입니다.** 텍스트를 읽고 텍스트를 씁니다. ②와 ⑤는 하네스 또는 API 서버가 하는 문자열 조립과 해체이고, ⑦은 완전히 내 코드입니다.

**②와 ⑤는 서로 반대 방향의 같은 변환입니다.** 우리가 보는 깔끔한 `tool_use` 블록은 원래 평문이었고, 우리가 보내는 깔끔한 `tools` 배열도 결국 평문이 됩니다. 양쪽 끝에서 JSON처럼 보이는 건 편의를 위한 포장입니다.

한 가지 중요한 성질이 여기서 나옵니다. **모델에는 상태가 없습니다.** ⑧에서 다시 ①로 갈 때, 이전 대화를 "기억하고 있으니 새 것만 보내면 되는" 게 아닙니다. 매번 처음부터 전부 다시 보냅니다. 도구 실행 결과를 대화에 다시 붙여야 하는 이유가 이것입니다 — 안 붙이면 모델은 그 결과의 존재를 알 방법이 없습니다.

---

## 4. 모델이 멈추는 지점

모델이 도구를 쓰겠다고 결정하면 응답이 끝나면서 **왜 끝났는지를 알려주는 값**이 같이 옵니다. Anthropic API에서는 `stop_reason`이라는 필드입니다.

| `stop_reason` | 의미 | 내가 할 일 |
|---|---|---|
| `end_turn` | 할 말을 다 하고 정상 종료 | 응답을 사용자에게 보여준다 |
| `tool_use` | 도구를 쓰겠다고 멈춤 | 실행하고 결과를 붙여 다시 호출 |
| `max_tokens` | 출력 길이 상한에 걸림 | 상한을 올리거나 스트리밍 |
| `stop_sequence` | 지정한 중단 문자열을 만남 | 상황에 따라 |
| `pause_turn` | 서버 측 도구가 반복 상한에 걸려 일시 정지 | 그대로 다시 보내면 이어서 진행 |
| `refusal` | 안전상의 이유로 거절 | 내용을 읽기 전에 먼저 이 값을 확인 |

`tool_use`로 멈췄을 때 응답 본문에는 `tool_use` 블록이 들어 있습니다.

```json
{
  "stop_reason": "tool_use",
  "content": [
    { "type": "text", "text": "날씨를 확인해 보겠습니다." },
    {
      "type": "tool_use",
      "id": "toolu_abc123",
      "name": "get_weather",
      "input": { "city": "서울" }
    }
  ]
}
```

`id`를 기억해 두십시오. 결과를 돌려줄 때 이 값으로 짝을 맞춥니다.

`content`가 배열이고 `text` 블록과 `tool_use` 블록이 같이 들어 있다는 점도 봐 둘 만합니다. 3절에서 본 대로 모델은 텍스트를 이어 쓸 뿐이므로, 도구 호출 앞에 자연어 문장을 먼저 쓰는 것도 자연스러운 일입니다. 파싱 단계가 그 흐름을 블록 두 개로 쪼갠 것뿐입니다.

---

## 5. 결과가 돌아가는 길

도구를 실행했으면 결과를 대화에 붙여서 다시 보냅니다. Anthropic API에서는 `tool_result` 블록을 **사용자 메시지 안에** 넣습니다.

```json
{
  "role": "user",
  "content": [
    {
      "type": "tool_result",
      "tool_use_id": "toolu_abc123",
      "content": "{\"city\": \"서울\", \"temp_c\": 29, \"condition\": \"흐림\"}"
    }
  ]
}
```

`tool_use_id`가 4절의 `id`와 같아야 합니다. 짝이 맞지 않으면 API가 거절합니다.

여기서 눈여겨볼 게 있습니다. **역할이 `user`입니다.** `tool`이라는 별도 역할이 아닙니다. 왜 그런지는 Qwen 템플릿을 다시 렌더링해 보면 눈으로 확인됩니다. 도구 호출과 실행 결과까지 붙여서 2차 요청을 만들면 이렇게 됩니다 (앞부분 시스템 메시지는 동일하므로 생략).

```text
<|im_start|>user
서울 날씨 알려줘<|im_end|>
<|im_start|>assistant
<tool_call>
{"name": "get_weather", "arguments": {"city": "서울"}}
</tool_call><|im_end|>
<|im_start|>user
<tool_response>
{"city": "서울", "temp_c": 29, "condition": "흐림"}
</tool_response><|im_end|>
<|im_start|>assistant
```

도구 결과가 `<|im_start|>user` 아래 `<tool_response>` 태그에 감싸여 들어갑니다. **프롬프트 층위에는 `tool`이라는 역할이 없고, 사용자 턴으로 위장해서 들어갑니다.**

이 사실은 그냥 흥미로운 디테일이 아닙니다. 도구가 외부에서 가져온 텍스트, 예를 들어 웹 페이지 내용이나 데이터베이스 조회 결과가 **사용자가 직접 말한 것과 같은 자리에 놓인다**는 뜻입니다. 그 텍스트에 "이전 지시를 무시하고 ○○해라" 같은 문장이 섞여 있으면 모델 입장에서는 사용자가 그렇게 말한 것과 구분되지 않습니다. 프롬프트 인젝션이 툴 콜링과 결합할 때 위험해지는 구조적 이유가 여기 있습니다. 별도 문서로 다룰 주제이지만, 툴 콜링을 이해했으면 이 위험도 함께 보여야 합니다.

---

## 6. 도구 설명문이 프롬프트인 이유

2절에서 확인한 대로 `description`은 프롬프트 본문에 그대로 찍힙니다. 그러니 도구를 고르는 과정에 특별한 선택 알고리즘 같은 건 없습니다. 모델은 프롬프트에 적힌 설명을 읽고, 지금 상황에 맞는 도구 이름을 다음 토큰으로 생성할 뿐입니다.

결과적으로 아래 두 정의는 모델에게 **완전히 다른 프롬프트**입니다.

```json
{ "name": "search", "description": "검색한다." }
```

```json
{ "name": "search_internal_docs",
  "description": "사내 위키와 설계 문서를 전문 검색한다. 사용자가 우리 팀의 결정 사항, 운영 절차, 과거 장애 기록을 물을 때 사용한다. 공개된 일반 지식에는 쓰지 않는다." }
```

두 번째가 더 잘 동작하는 이유는 모델이 더 똑똑해져서가 아니라, **프롬프트에 판단 근거가 더 많이 적혀 있어서**입니다. 이름도 마찬가지입니다. `search`보다 `search_internal_docs`가 나은 건 이름 자체가 이미 설명의 일부이기 때문입니다.

"언제 쓰는가"를 쓰는 게 특히 효과가 큽니다. 무엇을 하는 도구인지만 적으면 모델은 그 도구가 지금 필요한지를 스스로 추론해야 합니다. 반면 "사용자가 최근 사건이나 현재 가격을 물으면 호출하라"처럼 발동 조건을 적어 두면 그 추론 단계가 사라집니다.

반대 방향의 함정도 있습니다. `CRITICAL: 반드시 이 도구를 써야 한다` 같은 강한 표현은 모델이 필요 없는 상황에서도 도구를 부르게 만듭니다. 설명문은 프롬프트이므로 프롬프트를 쓸 때의 모든 감각이 그대로 적용됩니다.

---

## 7. 도구가 실패하면

실패해도 **무언가는 반드시 돌려줘야 합니다.** 응답을 생략하면 안 됩니다. `tool_use` 블록 하나에 `tool_result` 하나가 짝을 이루지 않으면 API가 다음 요청을 거절합니다.

Anthropic API에서는 `is_error` 플래그를 씁니다.

```json
{
  "type": "tool_result",
  "tool_use_id": "toolu_abc123",
  "content": "Error: 'xyz'라는 도시를 찾을 수 없습니다. 올바른 도시 이름을 지정하세요.",
  "is_error": true
}
```

오류 메시지를 성의 있게 쓰는 게 실질적으로 중요합니다. 5절에서 봤듯이 이 문자열은 다음 요청의 프롬프트 안으로 들어가고, 모델은 이걸 읽고 다음 행동을 정합니다. `Error: 500`만 돌려주면 모델이 할 수 있는 게 없습니다. 무엇이 잘못됐고 어떻게 고치면 되는지를 적으면 모델이 인자를 고쳐서 다시 호출하거나 사용자에게 되물을 수 있습니다.

**오류 메시지도 프롬프트입니다.** 6절과 같은 원리입니다.

---

## 8. 여러 도구를 한 번에 호출하면

모델은 한 응답에 `tool_use` 블록을 여러 개 담을 수 있습니다. Anthropic API에서 이 동작은 기본으로 켜져 있습니다.

이때 규칙이 하나 있고, 어기면 조용히 손해를 봅니다.

> **모든 `tool_result` 블록을 하나의 사용자 메시지에 담아서 보내야 합니다.**

나눠서 여러 메시지로 보내면 오류가 나지는 않습니다. 대신 **모델이 앞으로 병렬 호출을 하지 않도록 학습됩니다.** 대화 기록에 "여러 개를 요청했더니 하나씩 따로 돌아왔다"는 패턴이 남고, 모델은 그 패턴에 맞춰 다음부터 한 번에 하나씩만 요청합니다. 성능이 조용히 나빠지고, 로그만 봐서는 원인을 찾기 어렵습니다.

```json
{
  "role": "user",
  "content": [
    { "type": "tool_result", "tool_use_id": "toolu_001", "content": "..." },
    { "type": "tool_result", "tool_use_id": "toolu_002", "content": "..." },
    { "type": "tool_result", "tool_use_id": "toolu_003", "content": "..." }
  ]
}
```

하나만 실패했더라도 그 블록을 빼면 안 됩니다. `is_error: true`를 붙여서 같이 담습니다.

병렬 호출을 끄고 싶으면 `tool_choice`에 `disable_parallel_tool_use: true`를 넣습니다. `tool_choice`는 네 가지 값을 가집니다.

| 값 | 동작 |
|---|---|
| `{"type": "auto"}` | 모델이 알아서 판단 (기본값) |
| `{"type": "any"}` | 반드시 도구 중 하나를 쓴다 |
| `{"type": "tool", "name": "..."}` | 지정한 도구를 반드시 쓴다 |
| `{"type": "none"}` | 도구를 쓸 수 없다 |

---

## 9. 프롬프트 캐시와 만나는 지점

툴 콜링과 프롬프트 캐시는 별개 주제처럼 보이지만 한 지점에서 정면으로 만납니다. 이 절은 그 지점만 다룹니다. 캐시 자체의 원리는 별도 문서로 갑니다.

### 도구 정의는 프롬프트 맨 앞에 있다

2절에서 본 렌더링 결과를 다시 떠올려 보십시오. 도구 정의는 **시스템 메시지 안**, 즉 프롬프트의 가장 앞쪽에 있었습니다. Anthropic API의 렌더링 순서도 같습니다.

```text
[ tools ] ──> [ system ] ──> [ messages ]
   맨 앞                          맨 뒤
```

프롬프트 캐시는 **접두사가 완전히 같을 때만** 재사용됩니다. 앞쪽 한 글자가 바뀌면 그 뒤 전부가 무효가 됩니다. 도구 정의가 맨 앞에 있다는 사실과 이 규칙을 합치면 결론이 바로 나옵니다.

**도구 정의를 건드리면 캐시 전체가 날아갑니다.**

### 직접 측정한 결과

Qwen 템플릿으로 세 가지를 측정했습니다.

| 측정 | 결과 |
|---|---|
| 1차 요청(757자)이 2차 요청(975자)의 접두사인가 | **참** |
| 도구 설명문 끝에 느낌표 하나 추가 | 공통 접두사 757자 → **339자** |
| 시스템 프롬프트에 현재 시각 문자열 삽입 | **18번째 문자부터** 달라짐 |

첫 줄이 좋은 소식입니다. **툴 콜링 루프는 구조적으로 캐시 친화적입니다.** 도구 실행 결과는 대화 *끝에* 덧붙기 때문에 앞부분이 통째로 보존됩니다. 1차 요청 757자가 2차 요청의 접두사 그대로라는 건 그 757자를 다시 계산할 필요가 없다는 뜻입니다.

둘째 줄이 나쁜 소식입니다. 도구 설명문에 글자 하나를 더했을 뿐인데 공통 접두사가 절반 이하로 줄었습니다. 도구 정의가 앞에 있으니 그 뒤 400자가 전부 무효가 된 겁니다. 도구 목록을 사용자마다 다르게 조립하거나 대화 중간에 도구를 추가하면 매 요청이 캐시 미스입니다.

셋째 줄은 가장 흔한 실수입니다. 시스템 프롬프트에 `현재 시각은 ...`을 넣으면 매 요청마다 프롬프트 18번째 문자부터 달라지므로 사실상 캐시가 없는 것과 같습니다.

### 무효화에는 층위가 있다

모든 변경이 전부를 날리는 건 아닙니다. Anthropic API의 캐시는 세 층으로 나뉘고, 변경은 자기 층과 그 아래만 무효화합니다.

| 바꾼 것 | tools 캐시 | system 캐시 | messages 캐시 |
|---|:---:|:---:|:---:|
| 도구 정의 추가·삭제·순서 변경 | 무효 | 무효 | 무효 |
| 모델 교체 | 무효 | 무효 | 무효 |
| 시스템 프롬프트 내용 | 유지 | 무효 | 무효 |
| `tool_choice`, 이미지, thinking 켜고 끄기 | 유지 | 유지 | 무효 |
| 메시지 내용 추가 | 유지 | 유지 | 무효 |

**`tool_choice`를 매 요청 바꿔도 도구 정의와 시스템 프롬프트 캐시는 살아 있습니다.** 반면 도구 정의를 바꾸면 전부 다시 계산합니다. 이 표를 알면 "모드를 바꾸려고 도구 세트를 갈아 끼우는" 설계가 왜 비싼지, 대신 무엇을 해야 하는지가 보입니다 — 도구 세트를 고정해 두고 모드는 메시지 내용으로 전달하는 편이 훨씬 쌉니다.

### 에이전트 루프에만 있는 함정: 20블록 되돌아보기

캐시 지점(breakpoint)은 이전 캐시를 찾을 때 **최대 20개 콘텐츠 블록까지만 거슬러 올라갑니다.**

평범한 대화에서는 문제가 안 됩니다. 한 턴에 블록이 한두 개니까요. 그런데 에이전트 루프는 다릅니다. 도구를 여러 개 병렬로 부르면 `tool_use` 블록 N개와 `tool_result` 블록 N개가 한 턴에 쌓입니다. 몇 바퀴만 돌면 한 턴에 20블록을 넘기고, 그 순간 다음 요청의 캐시 지점이 이전 캐시를 **못 찾습니다.** 오류는 없고 그냥 조용히 미스입니다.

**툴 콜링을 이해해야만 보이는 캐시 함정이고, 캐시를 이해해야만 보이는 툴 콜링 비용입니다.** 두 개념이 만나는 자리가 정확히 여기입니다.

대응은 긴 턴 중간에 캐시 지점을 하나 더 두는 것입니다. 대략 15블록마다 한 번씩.

### 캐시가 걸렸는지 확인하는 법

응답의 `usage`를 봅니다.

| 필드 | 의미 |
|---|---|
| `cache_creation_input_tokens` | 이번에 캐시에 쓴 토큰 수 |
| `cache_read_input_tokens` | 이번에 캐시에서 읽은 토큰 수 |
| `input_tokens` | 캐시를 못 쓰고 새로 처리한 토큰 수 |

같은 접두사로 반복 요청했는데 `cache_read_input_tokens`가 계속 0이면 어딘가에 접두사를 깨는 요소가 있습니다.

주의할 점 하나. `input_tokens`는 **캐시에 걸리지 않은 나머지**입니다. 전체 프롬프트 크기가 아닙니다. 세 값을 더해야 전체입니다.

---

## 10. 흔한 오해

**"모델이 함수를 실행한다."** 아닙니다. 모델은 문자열을 생성하고 멈춥니다. 실행은 내 코드가 합니다. 3절.

**"도구 정의는 한 번만 보내면 된다."** 아닙니다. 모델에는 상태가 없어서 매 요청마다 전부 다시 보냅니다. 대화가 길어질수록 도구 정의도 매번 같이 실려 갑니다 — 캐시가 중요한 이유입니다. 3절, 9절.

**"도구 결과는 특별한 채널로 들어간다."** 아닙니다. 사용자 턴 안에 텍스트로 들어갑니다. 그래서 신뢰할 수 없는 텍스트를 도구가 물어 오면 위험합니다. 5절.

**"도구 설명문은 문서화용 주석이다."** 아닙니다. 프롬프트 본문입니다. 6절.

**"도구가 실패하면 결과를 안 보내면 된다."** 아닙니다. `is_error: true`를 붙여서 반드시 보냅니다. 7절.

**"병렬 호출 결과는 나눠 보내도 된다."** 오류는 안 나지만 모델이 병렬 호출을 그만두게 됩니다. 8절.

**"JSON 스키마를 주면 유효한 JSON이 보장된다."** 도구 정의만으로는 보장되지 않습니다. 2절에서 본 대로 템플릿은 형식을 *부탁*합니다. 진짜 보장은 샘플링 단계에서 문법에 안 맞는 토큰을 막아야 나오고, 그건 별도 주제입니다.

---

## 부록 A. 재현 방법

이 문서의 렌더링 결과와 측정값을 다시 만드는 방법입니다. 모델 가중치는 필요 없습니다. `tokenizer_config.json` 하나(약 7 KB)면 됩니다.

```bash
curl -sSL -o qwen_tokenizer_config.json \
  "https://huggingface.co/Qwen/Qwen2.5-7B-Instruct/resolve/main/tokenizer_config.json"
```

```python
# render.py — uv run --with jinja2 python render.py
import json
from jinja2 import Environment
from jinja2.ext import loopcontrols

cfg = json.load(open("qwen_tokenizer_config.json"))

# 템플릿이 tojson 필터를 쓰는데 jinja2 기본 제공이 아니라 직접 등록한다.
# ensure_ascii=False 를 줘야 한글이 \uXXXX 로 깨지지 않는다.
env = Environment(extensions=[loopcontrols], trim_blocks=True, lstrip_blocks=True)
env.filters["tojson"] = lambda o, **kw: json.dumps(o, ensure_ascii=False)
tpl = env.from_string(cfg["chat_template"])

TOOLS = [{
    "type": "function",
    "function": {
        "name": "get_weather",
        "description": "특정 도시의 현재 날씨를 조회한다.",
        "parameters": {
            "type": "object",
            "properties": {"city": {"type": "string", "description": "도시 이름. 예: 서울"}},
            "required": ["city"],
        },
    },
}]

turn1 = [{"role": "user", "content": "서울 날씨 알려줘"}]
turn2 = turn1 + [
    {"role": "assistant", "content": "",
     "tool_calls": [{"function": {"name": "get_weather", "arguments": {"city": "서울"}}}]},
    {"role": "tool", "content": '{"city": "서울", "temp_c": 29, "condition": "흐림"}'},
]

a = tpl.render(messages=turn1, tools=TOOLS, add_generation_prompt=True)
b = tpl.render(messages=turn2, tools=TOOLS, add_generation_prompt=True)
print(a)
print("1차가 2차의 접두사인가:", b.startswith(a), f"({len(a)}자 / {len(b)}자)")
```

**PASS 조건**: 출력 첫 부분에 `<tools>` 태그와 그 안의 도구 JSON이 보이고, 마지막 줄이 `<|im_start|>assistant` 로 끝나며, 접두사 판정이 `True`입니다.
**FAIL 신호**: `tojson` 관련 `TemplateError`가 나면 필터 등록이 빠진 것입니다. 한글이 `서울`처럼 보이면 `ensure_ascii=False`가 빠진 것입니다.

`chat_template` 필드가 없는 모델도 있습니다. 그런 경우 도구 호출을 지원하지 않거나 별도 파일에 템플릿이 있습니다.

### 확인한 것과 확인하지 못한 것

| 항목 | 상태 |
|---|---|
| 도구 정의가 시스템 메시지 안에 JSON 텍스트로 들어간다 | **직접 관측** (Qwen2.5-7B-Instruct 템플릿 렌더링) |
| 도구 결과가 `user` 턴의 `<tool_response>`로 들어간다 | **직접 관측** (같은 템플릿) |
| 1차 요청이 2차 요청의 접두사다 | **직접 측정** (`startswith` → True, 757/975자) |
| 설명문 한 글자 변경 시 접두사 757자 → 339자 | **직접 측정** |
| 시스템 프롬프트 시각 삽입 시 18번째 문자부터 변경 | **직접 측정** |
| 모델이 실제로 뱉는 원문 토큰 | **미검증.** 가중치를 돌리지 않았습니다. 템플릿이 지시하는 형식은 읽었지만 모델이 그 형식을 실제로 지키는지는 확인하지 못했습니다 |
| 상용 API 내부의 렌더링 결과 | **관측 불가.** 채팅 템플릿을 공개하지 않습니다. 변환은 서버 안에서 일어나고 우리는 양쪽 끝의 JSON만 봅니다 |

마지막 줄이 중요합니다. **2절과 5절에서 눈으로 본 텍스트는 열린 가중치 모델의 것입니다.** 상용 API도 같은 원리로 동작하지만 구체적인 문자열은 벤더마다 다르고 공개되지 않습니다. 그래서 이 층을 볼 수 있는 유일한 경로가 로컬에서 열린 모델을 돌려 보는 것입니다.

---

## 부록 B. 확인 시점과 출처

본문 1~3절과 6~8절 앞부분은 트랜스포머 추론 구조에서 나오는 성질이라 벤더가 바뀌어도 유지됩니다. 아래는 벤더별 구현이라 빠르게 바뀝니다.

**확인 시점: 2026-07-27**
**확인 방법: Anthropic Claude API 레퍼런스 (tool use, stop reasons, prompt caching)**

| 사실 | 값 |
|---|---|
| 도구 정의 필드 | `name`, `description`, `input_schema` (`tools` 배열의 최상위) |
| 모델의 도구 호출 블록 | `{"type": "tool_use", "id", "name", "input"}` |
| 멈춤 사유 필드 | `stop_reason` — `end_turn` / `tool_use` / `max_tokens` / `stop_sequence` / `pause_turn` / `refusal` |
| 결과 블록 | `{"type": "tool_result", "tool_use_id", "content"}`, 실패 시 `"is_error": true` |
| 결과를 담는 역할 | `user` |
| 병렬 호출 | 기본 활성. 모든 결과를 **하나의** user 메시지에 |
| 병렬 끄기 | `tool_choice`에 `"disable_parallel_tool_use": true` |
| 캐시 지정 | `"cache_control": {"type": "ephemeral"}`, 선택적 `"ttl": "1h"` |
| 캐시 지점 개수 | 요청당 최대 4개 |
| 렌더링 순서 | `tools` → `system` → `messages` |
| 되돌아보기 창 | 20 콘텐츠 블록 |
| 캐시 비용 | 읽기 약 0.1배, 쓰기 1.25배(5분 TTL) / 2배(1시간 TTL) |
| 손익분기 | 5분 TTL은 2회 재사용, 1시간 TTL은 3회 |
| 최소 캐시 크기 | 모델마다 다름. 512 / 1024 / 2048 / 4096 토큰 — **세대순으로 단조롭지 않음** |
| 확인 필드 | `usage.cache_read_input_tokens`, `usage.cache_creation_input_tokens`, `usage.input_tokens` |

최소 캐시 크기가 세대순이 아니라는 점은 직관에 어긋나니 적어 둡니다. 최신 모델이 512 토큰인데 한두 세대 전 모델이 4096인 경우가 있습니다. 3천 토큰짜리 프롬프트가 어떤 모델에서는 캐시되고 어떤 모델에서는 조용히 캐시되지 않습니다. 오류도 안 납니다.

또 하나. 이 표의 값은 **Anthropic 기준**입니다. 다른 벤더는 필드 이름이 다릅니다 (예: 결과를 담는 역할이 `tool`인 경우도 있습니다). 형태는 같지만 이름은 다르다는 게 이 계층의 성질입니다.

---

## 스스로 확인할 질문

문서를 덮고 답해 보십시오.

1. "모델이 도구를 호출한다"는 문장에서 실제로 모델이 하는 일은 정확히 무엇인가. 나머지는 누가 하는가.
2. 도구 정의는 매 요청마다 전송되는가. 그렇다면 왜 그런가.
3. 도구 실행 결과는 어떤 역할(role)로 대화에 들어가는가. 그 사실에서 어떤 보안 문제가 따라 나오는가.
4. 도구 세 개를 한 번에 호출당했는데 결과를 세 번에 나눠 보냈다. 오류는 나지 않았다. 무슨 일이 벌어지는가.
5. 대화만 길어지는 경우와 도구 설명문을 한 글자 고치는 경우 중, 프롬프트 캐시에 더 큰 타격을 주는 쪽은 어디이며 그 이유는 무엇인가.
