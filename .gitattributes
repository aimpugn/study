# 셸 스크립트는 항상 LF — core.autocrlf=true 환경에서 체크아웃 시 CRLF 로 바뀌면
# 리눅스 서버에서 "bad interpreter (^M)" 로 실행이 깨지는 것을 방지
*.sh text eol=lf
# 테스트 픽스처(/proc 스냅샷 모사)도 LF 고정 — CRLF 가 되면 gawk 필드에 \r 이 붙어 깨진다
*.snap text eol=lf
