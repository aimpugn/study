# 라이브 코딩 플레이북 (목요일 모의 + 금요일 아침에 펴 보기)

> 한 줄 진실: **"1시간 절기"는 몰라서가 아니라 혼자 침묵해서 생긴다. 입을 열면 안 생긴다.**
> 면접관은 심판이 아니라 페어다. 못 떠올려도 같이 풀어주고, 힌트는 정상 도구다.
> 평가 = 정답 < 과정·소통. 그들이 점수 주는 건 아래 의식 그대로다.

---

## 코딩 전 6줄 의식 (전부 소리 내어)

1. **명료화** — 입력/출력/제약/엣지를 면접관에게 되묻는다. (질문 = 점수 + 생각할 시간)
2. **브루트포스 + 복잡도** — "제일 단순한 완전탐색은 X, 복잡도 O(?), 제약 n=? 대입하면 통과? 초과?"
3. **초과면 병목** — "어디가 비싼가? 중복 계산? 매번 재스캔? 매번 정렬?"
4. **승격 체크포인트** — "방금 스친 생각이 답 아닌가?" 번뜩이면 **거기서 멈추고** 비용을 따진다.
5. **불변식 한 문장** — "i 처리 직후 ___는 항상 ___다."
6. **코드 = 그 문장 받아쓰기.** 짜면서 계속 내레이션.

2~4번이 평소 조용히 건너뛰던 칸이다. 입으로 밟으면 약점이 고쳐지고 동시에 점수가 된다.

---

## 막혔을 때 사다리 (위에서부터 차례로 — 절대 침묵 금지)

1. **막힌 지점을 말로** — "지금 X 때문에 막혔는데 Y랑 Z 고민 중이에요." (말하다 풀리는 경우 많음)
2. **작은 예제를 손으로** 굴린다 (n=2, 3).
3. **브루트포스라도 짜기 시작** — 느려도 도는 코드 ≫ 우아한 빈 화면.
4. **힌트 요청** — "이 방향 맞을까요?" / "힌트 하나 주실 수 있나요?" (감점 아님)
5. (허용되면) **검색** — 정당한 도구.

핵심: 너는 항상 뭔가 내놓을 수 있다 — 최소한 브루트포스. 10분 룰이 보장한다.

---

## 마음이 흔들릴 때 3개 리프레임

- **면접관 = 페어.** 끝까지 풀길 바라는 같은 편. (실제 후기: 못 떠올리자 같이 헷갈려주고 다른 길로 유도해 풀게 함)
- **브루트포스 먼저.** 완벽한 한 방이 아니라, 도는 것부터. 최적화는 그 다음. 비어 있는 1시간이 최악이지, 느린 정답은 최악이 아니다.
- **30초 이상 침묵 금지.** 생각도 소리 내어. 침묵이 유일한 진짜 실패다.

---

## 팩트 박스 (이번 라이브코딩 전형, 후기 종합)

- 1시간, 문제 2개, 1:1 화면공유(HackerRank CodePair).
- 난이도 LeetCode Medium 중심.
- 관찰된 유형: **BFS · Stack · Interval · String** (+ 구현/정렬/해시).
- 평가: 개발 능력만이 아니라 **문제 해결 과정 + 커뮤니케이션 + 프로그래밍 습관** 전체.

## 내가 이미 푼 유형 (자신감 근거)

Stack(괄호) · 격자 DFS(섬) · 슬라이딩 윈도우 · 정렬(퀵) · 힙(Jesse) · DP(Davis) · Interval(병합) · BFS(Castle).
관찰된 유형 거의 다 손에 있다.

---

## 금요일 전 체크리스트

- [ ] **HackerRank CodePair 미리 켜서 만져보기** (에디터, 실행 버튼, 단축키) — 도구 낯설면 그것만으로 버벅임.
- [ ] 치트시트(`cheatsheet/Cheatsheet.java`) 한 번 훑기 — PQ 정렬방향, ArrayDeque, stream, BFS/이분/투포인터/윈도우/DP 골격.
- [ ] 회고 카드(각 PROCESS.md의 `>` 줄) 인출 1회.
- [ ] 금요일 아침: **새 문제 금지.** 이 카드 + 6줄 의식만. 가볍게, 컨디션.
- [ ] 시작 멘트 준비: "먼저 입력/출력이랑 제약 좀 확인할게요." (명료화로 시작 = 점수 + 안정)
